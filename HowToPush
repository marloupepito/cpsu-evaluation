git init
git add .
git commit -m "hello"
git remote add origin https://github.com/marloupepito/cpsu-evaluation.git
git push -u origin master
git push -f origin master


git checkout -b main

git checkout master


heroku git:remote -a gould-care




 @vite(['resources/css/app.css', 'resources/js/app.js'])

at the student_subject_loading
if(student is exist student_subject_loading){
    if(student_subject_loading === 0){
        evaluation is done
    }else{
        continue to evaluation
    }

}else (is NOT EXIST){
    
    add data in student_subject_loading
    loop the available faculty by campusid, department/course, year and section   

}
